<div id="cookie-banner" class="cookie-banner" style="display: none;">
    <div class="cookie-content">
        <p>
            {{ trans('cookie.text') }}
            <a href="{{ route_localized('privacy', locale) }}">{{ trans('cookie.learn_more') }}</a>
        </p>
        <div class="cookie-actions">
            <button type="button" id="cookie-accept" class="btn btn-primary">{{ trans('cookie.accept') }}</button>
            <button type="button" id="cookie-decline" class="btn btn-secondary">{{ trans('cookie.decline') }}</button>
        </div>
    </div>
</div>
