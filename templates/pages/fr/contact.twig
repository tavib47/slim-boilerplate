{% extends 'layout/base.twig' %}

{% block title %}Contact{% endblock %}
{% block meta_description %}Contactez-nous{% endblock %}

{% block content %}
<section class="page-content">
    <h1>Contactez-nous</h1>
    <p class="lead">Vous avez des questions ? Nous serions ravis de vous entendre.</p>

    <form action="{{ route('contact') }}" method="post" class="mt-4" style="max-width: 600px;">
        <div class="mb-3">
            <label for="name" class="form-label">{{ trans('Name') }}</label>
            <input type="text" class="form-control" id="name" name="name" required>
        </div>

        <div class="mb-3">
            <label for="email" class="form-label">{{ trans('Email') }}</label>
            <input type="email" class="form-control" id="email" name="email" required>
        </div>

        <div class="mb-3">
            <label for="message" class="form-label">{{ trans('Message') }}</label>
            <textarea class="form-control" id="message" name="message" rows="5" required></textarea>
        </div>

        <button type="submit" class="btn btn-primary">{{ trans('Send') }}</button>
    </form>
</section>
{% endblock %}
